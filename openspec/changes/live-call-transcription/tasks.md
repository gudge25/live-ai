# Tasks

## 1. Каркас проєкту

- [x] 1.1 Створити pnpm-монорепо (`apps/server` на Node 24 LTS, `apps/extension` на WXT + React, `packages/shared`), TypeScript strict, ESLint, Vitest; перевірити `pnpm -r build` і `pnpm -r test` проходять
- [x] 1.2 Описати в `packages/shared` типи UI-подій (`session_started`, `partial`, `final`, `session_ended`, `status`) і zod-схеми; перевірити юніт-тестом парсинг валідних/невалідних подій
- [x] 1.3 Реалізувати завантаження та валідацію конфігу з env (D8), маскування секретів у логах; перевірити тестом, що відсутній `ARI_URL` дає зрозумілу помилку, а пароль не потрапляє в лог
- [x] 1.4 Додати `.env.example` і `docs/asterisk-setup.md` (http.conf TLS, ari.conf, модулі); перевірити, що файли є і всі змінні з конфігу задокументовані

- [x] 1.5 Spike на реальному Asterisk 18: через `curl` створити snoop на тестовому дзвінку + `externalMedia` (`encapsulation=audiosocket&transport=tcp&format=slin&data=<uuid>`) + mixing bridge і прийняти аудіо простим TCP-слухачем; зафіксувати в design.md, чи працює шлях D3, чи потрібен fallback через `Local`-канал

## 2. ARI-клієнт і моніторинг номерів

- [x] 2.1 Реалізувати REST-обгортку ARI (fetch, basic auth, TLS-опції) для channels/snoop/externalMedia/bridges/applications; перевірити тестами з mock-сервером
- [x] 2.2 Реалізувати WSS-клієнт подій з експоненційним backoff (≤30 с) і статусами `connected/reconnecting`; перевірити тестом з локальним ws-сервером, що після розриву відбувається reconnect
- [x] 2.3 Підписка на `endpoint:<TECH>/<ext>` для кожного номера після кожного підключення; перевірити тестом, що після reconnect підписки відновлюються
- [x] 2.4 Session manager: створення сесії на `Up`+у бриджі, дедуплікація, завершення на `ChannelDestroyed`/вихід із бриджу; перевірити тестами на записаних послідовностях ARI-подій (answer, ringing-no-answer, дублікати, hangup)

## 3. Підслуховування і AudioSocket

- [x] 3.1 AudioSocket TCP-сервер: парсер кадрів (UUID, audio, hangup, error), маршрутизація за UUID, відхилення невідомих UUID; перевірити юніт-тестами на синтетичних кадрах
- [x] 3.2 Tap orchestrator: 2× snoop (`spy=in`/`spy=out`), 2× externalMedia(audiosocket, slin), 2× mixing bridge з id-префіксом `liveai-`; перевірити інтеграційним тестом з mock ARI, що створюється рівно 6 ресурсів з правильними параметрами
- [x] 3.3 Прибирання ресурсів при кінці сесії та при помилці часткового створення; перевірити тестом, що після hangup і після збою на 3-му кроці не лишається ресурсів
- [x] 3.4 Прибирання осиротілих `liveai-*` каналів/бриджів на старті; перевірити тестом з mock ARI

## 4. Розпізнавання AssemblyAI

- [x] 4.1 Реалізувати адаптер `Transcriber` на SDK `assemblyai` (`streaming.transcriber`, sampleRate/encoding/languageCodes з конфігу, `formatTurns`, агрегація кадрів у чанки 50–100 мс, `close()` на кінці); перевірити юніт-тестом з замоканим SDK, що чанки мають правильний розмір і на hangup викликається `close()`
- [x] 4.2 Мапінг `Turn` → `partial`/`final` з `turn_order` і стороною; перевірити юніт-тестами на прикладах повідомлень
- [x] 4.3 Reconnect до 3 спроб і статус `transcription_degraded`; перевірити тестом з примусовим закриттям mock-сервера
- [ ] 4.4 Експеримент із dual-channel режимом (`AAI_DUAL_CHANNEL=true`): порівняти якість і атрибуцію з двома сесіями на тому самому записі; зафіксувати висновок у design.md
- [ ] 4.5 Ручна перевірка: прогнати записаний 8 kHz WAV через клієнт з реальним ключем і переконатися, що текст приходить з partial-затримкою < 1 с

## 5. Трансляція в UI

- [x] 5.1 WebSocket-ендпоінт `/ui` з перевіркою `token`, снапшотом активних сесій і фільтром за номером; перевірити тестами: невірний токен → закриття, новий клієнт отримує снапшот, фільтр працює
- [x] 5.2 In-memory сховище сесій з лімітом завершених; перевірити тестом, що старі сесії витісняються

## 6. Chrome-розширення

- [ ] 6.1 WXT-проєкт з entrypoints `sidepanel`, `options` і pop-out вікном (`chrome.windows.create`); перевірити, що розширення завантажується як unpacked без помилок і обидва вигляди відкриваються
- [ ] 6.2 Сторінка налаштувань (URL бекенду, токен, номери) у `chrome.storage.local`; перевірити, що значення зберігаються після перезапуску браузера
- [ ] 6.3 WS-клієнт з автоперепідключенням та індикатором стану; перевірити вручну вимкненням/увімкненням бекенду
- [ ] 6.4 Чат-подання діалогу: розрізнення agent/caller, стиль partial, заміна на final, автоскрол, позначка завершеної розмови; перевірити з mock-сервером подій (скрипт `pnpm dev:mock-events`)
- [ ] 6.5 Кнопка «Копіювати» у форматі `[час] Сторона: текст`; перевірити вставкою в текстовий редактор

## 7. Наскрізна перевірка і розгортання

- [x] 7.1 Dockerfile / systemd unit для сервера; перевірити запуск контейнера з `.env`
- [x] 7.2 E2E на тестовому Asterisk: дзвінок на 222 → у вікні з'являються репліки обох сторін; після hangup у `asterisk -rx "core show channels"` і `bridge show all` немає `liveai-*` ресурсів
- [ ] 7.3 Перевірити, що учасники дзвінка нічого не чують і розмова не переривається при зупинці сервісу посеред дзвінка
